<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path("home")}}">Welcome on Bar manager !</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
		<a class="nav-link {% if route_name and route_name == 'home' %}active{% endif %}" href="{{ path('home') }}">Home</a>
        </li>
         {% for category in categories %}
        <li class="nav-item">
          <a class="nav-link {% if category_id and category_id == category.id %}active{% endif %}" href="{{ path('show_beer_category', { id : category.id}) }}">{{category.name}}</a>
        </li>
        {% endfor %}
        <li class="nav-item">
            <a class="nav-link" href="{{ path("statistic") }}">Statistic</a>
        </li>
        {% if app.user %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path("app_logout") }}">logout</a>
            </li>
        {% else %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path("app_login") }}">login</a>
            </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>